/**
 * Generates a consumable string for the speech commands.
 * @param  {cmd} the speech command to be parsed
 * @return {string} consumable string
 */
const sanitizeCmd = cmd => {
  //--TODO regex operations
  return cmd
}
